<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exposes the downloaded update APK in the app cache to the package installer. -->
    <cache-path name="update_cache" path="updates/" />
    <external-files-path name="update_external" path="updates/" />
</paths>
